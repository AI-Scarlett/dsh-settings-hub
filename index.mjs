/**
 * Host half for DSH Settings Hub.
 *
 * The feature is intentionally browser-only. Keeping a no-op Host entry gives
 * the package a standard Bundle lifecycle without adding routes, services,
 * filesystem access, or Profile state.
 */
export function apply() {}

